#include "uclib.h"
#include "random.h"
#include <stdio.h>

void randomPhase(Real *result, int size)
{
    int i;
    for( i=0; i < size; ++i )
    {
        result[i] = 0.0;
    }
}

void randomAmplitude(Real *result, int size)
{
    int i;
    for( i=0; i < size; ++i )
    {
        result[i] = 0.0;
    }
}

